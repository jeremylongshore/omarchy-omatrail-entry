{
  "name": "omarchy-omatrail-entry",
  "version": "0.1.0",
  "private": true,
  "description": "Offline development and verification harness for the omaTrail Omarchy game",
  "scripts": {
    "test": "c8 --all --include=HuntingRules.js --include=JourneyRules.js --check-coverage --lines 95 --functions 95 --branches 90 --statements 95 node --test tests/*.test.js tests/*.suite.js",
    "test:race": "for run in 1 2 3; do node --test --test-concurrency=4 tests/*.test.js tests/*.suite.js || exit; done",
    "test:mutation": "stryker run",
    "test:balance": "node scripts/balance-sim.js --runs 100000 --output reports/balance/balance-100000.json",
    "test:e2e": "bash e2e/buzz.sh",
    "audit": "audit-harness verify && audit-harness audit . --deep && audit-harness scan ."
  },
  "devDependencies": {
    "@intentsolutions/audit-harness": "1.4.0",
    "@stryker-mutator/core": "^10.0.0",
    "c8": "^12.0.0"
  },
  "overrides": {
    "qs": "6.16.0"
  }
}
